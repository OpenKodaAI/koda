{
  "operation": "CircuitBreaker.run raising CircuitOpenError immediately while open",
  "measured_ns_authoring": 907,
  "max_ns": 10000,
  "rationale": "Fail-fast path. Measured 907ns vs 1500ms gRPC deadline = ~1.7M× speedup during a cascading-deadlock incident. Baseline 10000ns keeps the check stable on slower hosted runners while still catching state-machine cost growth well before it approaches 100us."
}
